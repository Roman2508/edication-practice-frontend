mutation ChangeStudentGroup($id: ID!, $group: ID!) {
  updateStudent(id: $id, data: { group: [$group] }) {
    data {
      id
      attributes {
        name
        email
        picture
        group {
          data {
            attributes {
              name
              courseNumber
            }
          }
        }
      }
    }
  }
}
